using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// When Colliding with the end  be able to : 
//Plant a seed
//Make it grow -> play anim 
//Depend on how much water you give it

// working with trigger? if u hit x then you won / be on last stone 

public class Winning : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
